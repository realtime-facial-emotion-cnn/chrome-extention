# Core dependencies for the FastAPI backend
fastapi>=0.100.0
uvicorn[standard]>=0.20.0
python-multipart>=0.0.6
pydantic>=2.0.0

# Optional: Uncomment when ready to use real implementations
# openai-whisper==20231117  # For audio transcription service
# tensorflow==2.15.0  # For emotion CNN model
# opencv-python-headless==4.10.0.84  # For video processing
# groq==0.9.0  # For LLM-powered summary generation
# python-dotenv==1.0.1  # For environment variables (.env file)
